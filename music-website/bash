# 1. Initialize Git (if you haven’t already)
git init

# 2. (Optional) Create or update .gitignore
echo "node_modules/" >> .gitignore

# 3. Stage all files
git add .

# 4. Commit
git commit -m "Initial site files"

# 5. Rename default branch to main
git branch -M main

# 6. Add the GitHub repo as remote
git remote add origin https://github.com/cjfouram/music-website.git

# 7. Push up to GitHub
git push -u origin main
